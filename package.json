{
  "name": "crudzaso",
  "version": "1.0.0",
  "description": "CRUDZASO - Task Management System based on Figma Design",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server --watch db.json --port 3000",
    "start": "concurrently \"npm run server\" \"npm run dev\""
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "concurrently": "^8.2.2"
  }
}
